const Header = ({titulo}) => {
    return (
        <header>
            {/*usando props <h1>{props.titulo}</h1>*/}
            <h1>{titulo}</h1>

        </header>
        
    )
}

export default Header